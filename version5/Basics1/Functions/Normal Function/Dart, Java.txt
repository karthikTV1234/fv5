ReturnType functionName(ParameterType parameter1, ParameterType parameter2) {
  // Function body
  return value; // (optional, only if ReturnType is not void)
}
Example:
int add(int a, int b) {
  return a + b;
}

Note: Java, Dart both are same.